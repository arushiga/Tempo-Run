import SwiftUI

enum TempoColor {
    static let ink = Color(red: 0.12, green: 0.16, blue: 0.24)
    static let slate = Color(red: 0.32, green: 0.38, blue: 0.49)
    static let primary = Color(red: 0.14, green: 0.39, blue: 0.89)
    static let secondary = Color(red: 0.07, green: 0.67, blue: 0.56)
    static let accent = Color(red: 0.62, green: 0.31, blue: 0.93)
    static let warmAccent = Color(red: 0.98, green: 0.52, blue: 0.24)
    static let surface = Color.white.opacity(0.78)
    static let surfaceStrong = Color.white.opacity(0.92)
    static let line = Color.white.opacity(0.6)
    static let backgroundTop = Color(red: 0.94, green: 0.96, blue: 1.0)
    static let backgroundBottom = Color(red: 0.99, green: 0.95, blue: 0.92)
}
